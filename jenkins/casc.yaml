jenkins:
  systemMessage: "Traverse CI"
  # Docker Desktop's VM here only has ~7.4GB total, shared across the whole
  # already-running stack (14 containers) plus whatever this build spawns.
  # The default 2 executors let the matrix stage's 6 backend builds run
  # 3-wide in parallel, which was enough concurrent JVM/Maven memory
  # pressure to get a build's own child process OOM-killed silently
  # (Jenkins kept reporting "building" for a process that no longer
  # existed). Pinning to 1 executor forces every parallel stage --
  # the matrix cells, most notably -- to run one at a time instead.
  numExecutors: 1
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "admin"
          password: "${JENKINS_ADMIN_PASSWORD}"
  authorizationStrategy:
    loggedInUsersCanDoAnything:
      allowAnonymousRead: false

credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope: GLOBAL
              id: "github-credentials"
              username: "AliHJMM"
              password: "${GITHUB_TOKEN}"
              description: "GitHub PAT -- repo checkout + commit status reporting"
          - string:
              scope: GLOBAL
              id: "sonarqube-token"
              secret: "${SONARQUBE_TOKEN}"
              description: "SonarQube analysis token"

unclassified:
  location:
    url: "http://localhost:8090/"
  sonarGlobalConfiguration:
    installations:
      - name: "SonarQube"
        serverUrl: "http://sonarqube:9000"
        credentialsId: "sonarqube-token"

# Multibranch (not a single pipelineJob) so every branch AND every PR gets
# built automatically -- the github-branch-source plugin reports a real
# GitHub Checks status per head it builds, which is what branch protection's
# "require status checks to pass" gates on. Periodic scan (every 2 minutes)
# is the multibranch equivalent of classic Poll SCM -- no public webhook
# endpoint needed since Jenkins is doing the asking, not GitHub.
jobs:
  - script: >
      multibranchPipelineJob('traverse-pipeline') {
        branchSources {
          branchSource {
            source {
              github {
                id('traverse-github-source')
                configuredByUrl(true)
                repositoryUrl('https://github.com/AliHJMM/Traverse')
                repoOwner('AliHJMM')
                repository('Traverse')
                credentialsId('github-credentials')
                traits {
                  gitHubBranchDiscovery {
                    strategyId(1)
                  }
                  gitHubPullRequestDiscovery {
                    strategyId(1)
                  }
                }
              }
            }
          }
        }
        factory {
          workflowBranchProjectFactory {
            scriptPath('jenkins/Jenkinsfile')
          }
        }
        triggers {
          periodicFolderTrigger {
            interval('2m')
          }
        }
      }
